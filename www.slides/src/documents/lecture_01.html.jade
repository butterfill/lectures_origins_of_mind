---
layout: 'default2'
title: 'Lecture 01'
weburl: ''
units: []
---

include ../../fragments/slide_mixins

- var images = get_images('bees')

mixin title_slide({document:document, images:images})

// the question
mixin insert_unit({unit:'001', title_slide:false, images:images, handout:true})
 
// myths to mechanisms
mixin insert_unit({unit:'021', title_slide:true, images:images})

//inbetween mindless behaviour and thought (object permanence)
mixin insert_unit({unit:'031', title_slide:true, images:images})




mixin section({title:'Admin', images:images, handout:false})
section.slide
  .words: .container_12: .grid_12
    p.dimafter lectures
    p.slide seminars
    p.slide seminar groups
    p.slide seminar tasks
    p.slide unassessed essay
    p.slide first seminar task


//- two breakthroughs
mixin insert_unit({unit:'051', title_slide:true, images:images, handout:false})

// first breakthrough: social interaction
mixin insert_unit({unit:'061', title_slide:true, images:images})

// second breakthrough: core knowledge
mixin insert_unit({unit:'071', title_slide:true, images:images})

// bringing core knowledge and social interaction together
mixin insert_unit({unit:'081', title_slide:true, images:images, handout:false})


//-
  *todo*: development as rediscovery insert?

//- 
  background units needed
  // representation 
  mixin insert_unit({unit:'091', title_slide:true, images:images})

  // methods:
    - habituation
    - violation of expectations
    - EEG


//-
  // more on object permanence
  mixin insert_unit({unit:'101', title_slide:true, images:images})

//-
  spelke on principles of object perception
  use espp unperceived objects : this is about inference 
  the principles:
    cohesion—“two surface points lie on the same object only if the points are linked by a path of connected surface points”
    boundedness—“two surface points lie on distinct objects only if no path of connected surface points links them … the boundedness principle implies that … two distinct objects cannot interpenetrate.”
    rigidity—“objects are interpreted as moving rigidly if such an interpretation exists”
    no action at a distance—“separated objects are interpreted as moving independently of one another if such an interpretation exists”





//mixin section({title:'*todo*', images:images})
//
//section.slide
//  .words: .container_12: .grid_12
//    p *todo*
//    ol
//      li What is representation? Dretske  [Not sure where this should go]
//      li nb: representation matters for the view about language acquisition.
//        span If Fodor on representation then the Bloom private language view looks more plausible.
//      li Core knowledge (first breakthrough) [use Spelke on principles of object perception]
//      li Social Interaction (second breakthrough)---use the stuff on word learning from Chapter_02



